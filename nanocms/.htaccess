# NanoCMS v0.4 SEF url's mod_rewrite file
RewriteEngine On
RewriteBase /dvdbrowsereview/nanocms
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?page=$1 [L]
